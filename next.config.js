/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'image.tmdb.org',
      's4.anilist.co',
      'img1.ak.crunchyroll.com',
    ],
  },
}

module.exports = nextConfig
